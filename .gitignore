node_modules
dist
.env
.env.local
*.log

# Real roster pulled for local testing only. Contains student PII —
# must never be committed or deployed.
public/local-roster/

# Local-only config override (points at real data during development).
public/config.local.json

# Local Netlify folder
.netlify
